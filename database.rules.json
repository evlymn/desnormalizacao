{
  "rules": {
    ".read": true,
    ".write": true,
    "cartoes": {
      ".indexOn": "nome",
      "$id": {
        ".validate": "newData.child('nome').exists() && newData.child('cvv').isNumber() && root.child('bandeiras').child(newData.child('bandeira').val()).exists()"
      }
    }
  }
}
